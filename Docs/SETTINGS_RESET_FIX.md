# 🔧 Фікс проблеми скидання налаштувань обладнання

## 🐛 Проблема

Налаштування обладнання (scale, scanner, printer) періодично скидалися до дефолтних значень через кілька критичних помилок у коді.

## 🔍 Виявлені причини

### 1. **Проблема з частковим оновленням (Partial Updates)**

**Старий код:**
```typescript
async updateScaleSettings(scaleSettings: Partial<EquipmentSettings['scale']>): Promise<void> {
  await this.saveScaleSettings(scaleSettings as EquipmentSettings['scale']);
}
```

**Проблема:**
- Метод приймає `Partial<>` об'єкт (не всі поля присутні)
- Робить **небезпечний cast** до повного типу
- Викликає `saveScaleSettings`, який використовує `??` (nullish coalescing) з дефолтними значеннями
- Якщо поле `undefined`, воно замінюється дефолтним значенням, **навіть якщо в БД є користувацьке значення**

### 2. **Відсутність читання поточних налаштувань**

**Старий код:**
```typescript
const scaleSettingsList = [
  {
    key: 'equipment_scale.baudRate',
    value: JSON.stringify(scaleSettings.baudRate ?? 4800), // ❌ Якщо undefined → 4800
    description: 'Швидкість передачі даних ваг'
  },
  // ...
];
```

**Проблема:**
- Не читаються поточні значення з БД перед оновленням
- Оператор `??` замінює `undefined` дефолтним значенням
- **Втрачаються користувацькі налаштування**, якщо поле не передано

### 3. **Перезапис цілих секцій при частковому оновленні**

**Приклад:**
```typescript
// Користувач змінює тільки baudRate
await updateScaleSettings({ baudRate: 9600 });

// Старий код зберігає ВСІ поля:
// baudRate: 9600 ✅
// dataBits: 8 (дефолт) ❌ - було 7 в БД
// stopBits: 1 (дефолт) ❌ - було 2 в БД
// ... інші поля скидаються до дефолтів
```

## ✅ Рішення

### 1. **Читання поточних налаштувань перед оновленням**

```typescript
async updateScaleSettings(scaleSettings: Partial<EquipmentSettings['scale']>): Promise<void> {
  // ✅ CRITICAL FIX: Читаємо поточні налаштування
  const currentSettings = await this.getEquipmentSettings();
  
  // ✅ Мержимо з новими (оновлюємо тільки передані поля)
  const mergedScaleSettings = {
    ...currentSettings.scale,
    ...scaleSettings
  };
  
  await this.saveScaleSettings(mergedScaleSettings);
}
```

### 2. **Збереження тільки переданих полів**

```typescript
private async saveScaleSettings(scaleSettings: EquipmentSettings['scale']): Promise<void> {
  const scaleSettingsList = [];
  
  // ✅ Зберігаємо тільки поля, які реально передані (не undefined)
  if (scaleSettings.baudRate !== undefined) {
    scaleSettingsList.push({
      key: 'equipment_scale.baudRate',
      value: JSON.stringify(scaleSettings.baudRate), // Без ?? оператора!
      description: 'Швидкість передачі даних ваг'
    });
  }
  // ... інші поля аналогічно
  
  if (scaleSettingsList.length > 0) {
    await this.batchUpsertSettings(scaleSettingsList);
  }
}
```

### 3. **Захист від порожніх секцій**

```typescript
async saveEquipmentSettings(settings: EquipmentSettings): Promise<void> {
  // ✅ Логування для відстеження змін
  console.log('📋 Налаштування для збереження:', {
    scale: settings.scale ? 'присутні' : 'відсутні',
    scanner: settings.scanner ? 'присутні' : 'відсутні'
  });

  // ✅ Зберігаємо тільки присутні секції
  const savePromises = [];
  
  if (settings.scale) {
    savePromises.push(this.saveScaleSettings(settings.scale));
  }
  if (settings.scanner) {
    savePromises.push(this.saveScannerSettings(settings.scanner));
  }
  // ...
  
  await Promise.all(savePromises);
}
```

## 📊 Порівняння поведінки

### До фіксу:
```typescript
// Користувач має в БД:
// scale.baudRate = 9600
// scale.dataBits = 7

// Викликає часткове оновлення:
await updateScaleSettings({ baudRate: 4800 });

// Результат в БД:
// scale.baudRate = 4800 ✅
// scale.dataBits = 8 ❌ (скинуто до дефолту замість 7)
```

### Після фіксу:
```typescript
// Користувач має в БД:
// scale.baudRate = 9600
// scale.dataBits = 7

// Викликає часткове оновлення:
await updateScaleSettings({ baudRate: 4800 });

// Результат в БД:
// scale.baudRate = 4800 ✅
// scale.dataBits = 7 ✅ (збережено користувацьке значення)
```

## 🎯 Змінені методи

### `EquipmentSettingsService`:
- ✅ `updateScaleSettings()` - додано читання поточних налаштувань + merge
- ✅ `updateScannerSettings()` - додано читання поточних налаштувань + merge
- ✅ `updatePrinterSettings()` - додано читання поточних налаштувань + merge
- ✅ `saveScaleSettings()` - видалено `??` оператори, перевірка на `undefined`
- ✅ `saveScannerSettings()` - видалено `??` оператори, перевірка на `undefined`
- ✅ `savePrinterSettings()` - видалено `??` оператори, перевірка на `undefined`
- ✅ `saveOrderSoundSettings()` - додано фільтрацію `undefined` значень
- ✅ `saveEquipmentSettings()` - додано перевірки на наявність секцій

## 🔍 Моніторинг

Додано логування для відстеження змін:

```typescript
console.log('📋 Налаштування для збереження:', {
  scale: settings.scale ? 'присутні' : 'відсутні',
  scanner: settings.scanner ? 'присутні' : 'відсутні',
  printer: settings.printer ? 'присутні' : 'відсутні'
});
```

Це допоможе швидко виявити проблеми, якщо вони виникнуть знову.

## 🛡️ Захист від повторення

### Правила для нових методів:

1. **НІКОЛИ** не використовуйте `??` оператор з дефолтними значеннями при збереженні налаштувань
2. **ЗАВЖДИ** читайте поточні налаштування перед частковим оновленням
3. **ЗАВЖДИ** перевіряйте `!== undefined` перед збереженням поля
4. **ЗАВЖДИ** мержте часткові оновлення з поточними налаштуваннями

### Безпечний паттерн:

```typescript
// ✅ ПРАВИЛЬНО
async updateSettings(partial: Partial<Settings>): Promise<void> {
  const current = await this.getSettings();
  const merged = { ...current, ...partial };
  await this.saveSettings(merged);
}

private async saveSettings(settings: Settings): Promise<void> {
  const list = [];
  if (settings.field !== undefined) {
    list.push({ key: 'field', value: JSON.stringify(settings.field) });
  }
  await this.batchUpsert(list);
}

// ❌ НЕПРАВИЛЬНО
async updateSettings(partial: Partial<Settings>): Promise<void> {
  await this.saveSettings(partial as Settings); // Небезпечний cast!
}

private async saveSettings(settings: Settings): Promise<void> {
  const list = [{
    key: 'field',
    value: JSON.stringify(settings.field ?? DEFAULT_VALUE) // Втрата даних!
  }];
  await this.batchUpsert(list);
}
```

## 📝 Тестування

### Ручне тестування:

1. Встановіть кастомні значення в налаштуваннях ваг:
   ```typescript
   await updateScaleSettings({
     baudRate: 9600,
     dataBits: 7,
     stopBits: 2
   });
   ```

2. Оновіть тільки одне поле:
   ```typescript
   await updateScaleSettings({ baudRate: 4800 });
   ```

3. Перевірте, що інші поля НЕ скинулися:
   ```typescript
   const settings = await getEquipmentSettings();
   console.log(settings.scale.dataBits); // Має бути 7, а не 8
   console.log(settings.scale.stopBits); // Має бути 2, а не 1
   ```

## 🚀 Deployment

Після оновлення коду:
1. Перезапустіть сервер
2. Перевірте логи на наявність `📋 Налаштування для збереження:`
3. Протестуйте часткове оновлення налаштувань через UI
4. Переконайтеся, що налаштування не скидаються

## 📚 Додаткова інформація

- **Дата фіксу:** 3 грудня 2025
- **Файли:** `server/services/settingsService.ts`
- **Версія:** v1.0.0
- **Автор фіксу:** GitHub Copilot

---

**Важливо:** Цей фікс критично важливий для стабільності системи. Без нього користувацькі налаштування обладнання будуть періодично скидатися, що може призвести до збоїв у роботі ваг, сканерів та принтерів.
