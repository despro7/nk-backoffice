// Типи для ToastService (toast-сповіщення)

export interface ToastSettingsTypes {
  apiErrors: boolean;
  authErrors: boolean;
  authSuccess: boolean;
  equipmentStatus: boolean;
  systemNotifications: boolean;
  tokenExpiry: boolean;
  tokenRefresh: boolean;
}
